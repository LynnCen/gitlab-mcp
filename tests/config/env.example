# GitLab MCP 测试环境配置

# GitLab 实例地址 (必需)
GITLAB_HOST=

# GitLab 个人访问令牌 (必需)
GITLAB_TOKEN=

# 调试模式 (可选)
# 设置为 1 或 true 启用详细日志
DEBUG=false

# 日志级别 (可选)
# 可选值: info, debug, error
LOG_LEVEL=info

# 使用说明:
# 1. 复制此文件到项目根目录并重命名为 .env
# 2. 填入真实的 GitLab 配置信息
# 3. 运行测试: npm run test:quick 