# GitLab MCP 服务器配置示例
# 复制此文件为 .env 或直接设置环境变量

# GitLab 实例地址 (必需)
GITLAB_HOST=https://gitlab.com

# GitLab 访问令牌 (必需)
# 获取方式：GitLab → 用户设置 → Access Tokens → 创建个人访问令牌
# 需要权限：api, read_user, read_repository
GITLAB_TOKEN=glpat-xxxxxxxxxxxxx

# 请求超时时间 (毫秒，可选)
GITLAB_TIMEOUT=30000

# 重试次数 (可选)
GITLAB_RETRIES=3 